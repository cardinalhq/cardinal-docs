import SupportCallout from '../../components/SupportCallout';

# Quick Start has moved

The Helm quick start now lives on the **[Manual install](/lakerunner/manual-install)** page.

For most deployments we recommend letting Cardinal install and manage Lakerunner for you — see
**[Install Lakerunner](/lakerunner/install)**.

- **[Install Lakerunner](/lakerunner/install)** — operator-managed (recommended)
- **[Manual install (Helm)](/lakerunner/manual-install)** — run the chart yourself, good for a POC

<SupportCallout />
