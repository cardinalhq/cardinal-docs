import SupportCallout from '../../components/SupportCallout';

# Quick Start has moved

The Helm quick start now lives on the **[Manual install](/data-lake/manual-install)** page.

For most deployments we recommend letting Cardinal install and manage Cardinal Data Lake for you — see
**[Install Cardinal Data Lake](/data-lake/install)**.

- **[Install Cardinal Data Lake](/data-lake/install)** — operator-managed (recommended)
- **[Manual install (Helm)](/data-lake/manual-install)** — run the chart yourself, good for a POC

<SupportCallout />
